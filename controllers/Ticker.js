import NewTicker from "../models/NewTicker.js";

export const getTickers = (req, res) => {
  try {

  } catch (error) {
    
  }
}

export const newTicker = (req, res) => {
  res.send("New Ticker Created");
}